This is a project create using Java and MYSQL with wampserver. This project can be cloned using website given in the block. Files required to run the project is given in the
master folder itself. It requires two additional libraries which are present in the folder. The database file is also given in th folder. You just have to copy and paste it into
the mysql command line.

THANKS!!!
